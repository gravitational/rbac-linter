kind: role
version: v4
metadata:
  name: example-role
spec:
  allow:
    node_labels:
      'env'   : 'aa*'
      'owner' : '{{internal.key2}}'
      'foobar' : 'asdf'

  deny:
    node_labels:
      'workload': ['database', 'backup']
