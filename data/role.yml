kind: role
version: v4
metadata:
  name: example-role
spec:
  allow:
    node_labels:
      'env'   : 'a*a'
      'owner' : '{{internal.key1}}'
      'fooba' : 'asdf'

  deny:
    node_labels:
      'workload': ['database', 'backup']
