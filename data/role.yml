kind: role
version: v4
metadata:
  name: example-role
spec:
  allow:
    node_labels:
      'env'   : 'a*a'
      'owner' : '{{regexp.replace(   external.email  ,   "foo"  ,   "bar"   )}}'
      'foobar' : 'asdf'

  deny:
    node_labels:
      'workload': ['database', 'backup']
